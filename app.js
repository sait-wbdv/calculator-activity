"use strict";

// calculation values
const num1 = 8;
const num2 = 33;
const operator = "+";

const add = num1 + num2;

const subtract = num1 - num2;

const divide = num1 / num2;

const multiply = num1 * num2;

// add the your calculations to a conditional statement

if (/* operator is equal to + */) {
  // return the addition equation 
} else if (/* operator is equal to - */) {
  // return the subtraction equation
} else if (/* operator is equal to / */) {
  // return the division equation
} else if (/* operator is equal to * */) {
  // return the multiplication equation
} else {
  console.error("the operator must be either + - / or * ")
  return 0;
}